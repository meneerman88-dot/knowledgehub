# Placeholder voor Task 4.
# In Task 4 vullen we deze GitHub Actions workflow verder in.

name: Knowledge Hub CI

on:
  push:
    branches:
      - main

jobs:
  basic-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show project structure
        run: ls -R
